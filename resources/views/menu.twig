<div id="{{ id }}" class="collapse navbar-collapse navbar-ex1-collapse">
    <ul class="nav navbar-nav side-nav">
        {% for item in items %}
            {{ item.render() }}
        {% endfor %}
    </ul>
</div>
